# Two Sum LeetCode Problem

This project implements the 'Two Sum' problem from LeetCode using Java and Maven.

## Usage

1. Build the project using Maven:
   ```bash
   mvn clean install
   ```
2. Run the tests:
   ```bash
   mvn test
   ```